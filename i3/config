# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Solution for the issue with NVIDIA card and black screen on loading i3wm.
exec xrandr --setprovideroutputsource modesetting NVIDIA-0
exec xrandr --auto
# endof solution.

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10
# font xft:Droid Sans Mono 9
font xft:Comic Shanns 12

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# gaps

# start a terminal
# bindsym $mod+Return exec i3-sensible-terminal -e /bin/zsh
bindsym $mod+Return exec gnome-terminal -e /bin/zsh

# kill focused window
bindsym $mod+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run
# start rofi (better drun)
bindsym $mod+space exec --no-startup-id rofi -show drun

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+semicolon split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+Tab focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1:main
bindsym $mod+2 workspace 2:www
bindsym $mod+3 workspace 3:code
bindsym $mod+4 workspace 4:msg
bindsym $mod+5 workspace 5:media
#bindsym $mod+6 workspace 6:misc
bindsym $mod+7 workspace 7:obsi
#bindsym $mod+8 workspace 8:misc
#bindsym $mod+9 workspace 9:misc
bindsym $mod+0 workspace 10:term

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1:main
bindsym $mod+Shift+2 move container to workspace 2:www
bindsym $mod+Shift+3 move container to workspace 3:code
bindsym $mod+Shift+4 move container to workspace 4:msg
bindsym $mod+Shift+5 move container to workspace 5:media
#bindsym $mod+Shift+6 move container to workspace 6:misc
bindsym $mod+Shift+7 move container to workspace 7:obsi
#bindsym $mod+Shift+8 move container to workspace 8:misc
#bindsym $mod+Shift+9 move container to workspace 9:misc
bindsym $mod+Shift+0 move container to workspace 10:term

# move with mod+ctrl and switch workspaces
bindsym $mod+Control+Left workspace prev
bindsym $mod+Control+Right workspace next

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	font xft:Droid Sans Mono 9 
	status_command i3status --config ~/.config/i3/i3status.conf
	tray_output primary
  position top
}

# bar {
#  	font pango:DejaVu Sans Mono, Dejavu, Awesome 8
# 	status_command i3blocks -c ~/.config/i3/i3blocks.conf
# 	colors {
#     separator #268bd2
#     background #002b36
#     statusline #839496
#     focused_workspace #fdf6e3 #6c71c4 #fdf6e3
#     active_workspace #fdf6e3 #6c71c4 #fdf6e3
#     inactive_workspace #002b36 #586e75 #002b36
#     urgent_workspace #d33682 #d33682 #fdf6e3
#   }
# }

# alias xp='xprop | grep "WM_WINDOW_ROLE\|WM_CLASS" && echo "WM_CLASS(STRING) = \"NAME\", \"CLASS\""'

workspace_layout tabbed
workspace_auto_back_and_forth yes
force_focus_wrapping yes
assign [class="Google-chrome"] 2:www
assign [class="firefox"] 2:www
# assign [title="newsbeuter"] 2:www
# assign [class="sxiv"] 5:media
# assign [class="MPlayer"] 5:media
# assign [class="Skype"] 4:msg
 assign [class="Slack"] 4:msg
 assign [class="TelegramDesktop"] 4:msg
#assign [title="finch"] 4:msg
# assign [class="URxvt"] 10:term
# assign [class="gnome-terminal"] 10:term
assign [class="Gnome-terminal"] 10:term
assign [class="Gedit"] 3:code
assign [class="jetbrains-phpstorm"] 3:code
assign [class="Insomnia"] 3:code
assign [class="Code"] 3:code
for_window [class="()"] focus
# for_window [class="URxvt"] focus
assign [class="Doublecmd"] 1:main
assign [class="Pcmanfm"] 1:main
assign [class="Nautilus"] 1:main
assign [class="obsidian"] 7:obsi

# for_window [class="Skype"] floating enable
for_window [class="Pcmanfm"] floating enable border pixel 1
for_window [class="Nautilus"] floating enable border pixel 1
for_window [class="feh"] floating enable border pixel 1
for_window [class="Gnome-terminal"] border pixel 1 
for_window [class="TelegramDesktop"] border pixel 1 
for_window [class="Slack"] border pixel 1 
# for_window [class="Mplayer"] floating enable

 # see alsamixer -- sound card device
 bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -q -D default set Master 5%+ unmute
 bindsym XF86AudioLowerVolume exec --no-startup-id amixer -q -D default set Master 5%- unmute
 bindsym XF86AudioMute exec --no-startup-id amixer -q -D default set Master toggle

 bindsym XF86AudioPlay exec --no-startup-id playerctl play
 bindsym XF86AudioNext exec --no-startup-id playerctl next
 bindsym XF86AudioPrev exec --no-startup-id playerctl previous
 bindsym XF86AudioStop exec --no-startup-id playerctl stop
 bindsym XF86AudioPause exec --no-startup-id playerctl pause
bindsym $mod+Shift+Return exec i3-sensible-terminal -e vim

bindsym $mod+Shift+p exec "i3-nagbar -t warning -m 'You pressed the poweroff shortcut. Do you really want to poweroff?' -b 'Yes, #poweroff' 'systemctl poweroff'"
#bindsym Print exec --no-startup-id "scrot '%F_%H%M%S_$wx$h.png' -e 'mv $f ~/screens/'"

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+s exec --no-startup-id "i3-nagbar -t warning -f -misc-fixed-medium-r-normal-*-15-140-75-75-c-90-iso10646-1 -m 'Вийти з i3? Це беззаперечно зупинить Х-сесію!' -b 'Так, вийти з i3' 'i3-msg exit'"

# lock screen
#bindsym $mod+Control+l exec i3lock -c 323232
#bindsym $mod+Control+l exec lock -gp
bindsym $mod+Control+l exec /home/ryba/.config/i3/./lock.sh

# off touchpad
# bindsym $mod+Control+p exec synclient touchpadoff=1

# custom
bindsym $mod+t border toggle

# Screenshot
bindsym $mod+Shift+8 exec /usr/bin/flameshot gui

# second home monitor
# exec_always --no-startup-id xrandr --output HDMI2 --mode 1920x1080 --right-of HDMI1

exec --no-startup-id "setxkbmap -layout us,ua"
exec --no-startup-id "setxkbmap -option 'grp:alt_shift_toggle'"
exec "setxkbmap -layout us,ua"
exec "setxkbmap -option 'grp:alt_shift_toggle'"

exec --no-startup-id nitrogen --restore
#exec --no-startup-id kupfer
exec --no-startup-id parcellite
# exec --no-startup-id xmodmap -e "pointer = 3 2 1";
exec --no-startup-id guake


# dependencies:
# sudo apt install playerctl
# sudo apt install nitrogen
# sudo apt install feh

# sudo apt install rofi
# mkdir -p ~/.config/rofi
# rofi -dump-config > ~/.config/rofi/config.rasi

# notes
# showkey
# xev
# xmodmap -pke
# alias -- xp (whoch window CLASS)
